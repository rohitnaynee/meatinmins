import React from 'react';

import './exotic.styles.scss';

const Exotic = () => (
    <div className = 'mutton' >
      <h1 className = 'text'>
       Sorry again, exotic is bit hard to get.!
      </h1>
      <h3>
       we cannot promise that we will resolve as early as possible       
      </h3>
    </div>
);

export default Exotic;